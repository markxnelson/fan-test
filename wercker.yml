box: alpine

build:
  steps:

echoa:
  steps:
    - script:
        name: echo AA
        code: echo 'AA' > forward.txt

readfile:
  steps:
    - script:
        name: read forward.txt
        code: cat forward.txt
