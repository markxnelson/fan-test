box: alpine

build:
  steps:

echoa:
  steps:
    - script:
        name: echo AA
        code: echo 'AA' > forward.txt

echob:
  steps:
    - script:
        name: echo BBB
        code: echo 'BBB' > forward.txt

readfile:
  steps:
    - script:
        name: read forward.txt
        code: cat forward.txt

readfile2:
  steps:
    - script:
        name: read forward.txt
        code: cat forward.txt && cat forward.txt
